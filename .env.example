# Generic LLM Configuration (Recommended)
LLM_API_KEY=your-api-key-here
LLM_PROVIDER=openai
LLM_MODEL=gpt-4o-mini
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=2000
# LLM_BASE_URL=https://api.openai.com/v1  # Optional custom endpoint

# Backward Compatibility (Legacy)
OPENAI_API_KEY=your-openai-api-key-here

# Instructions:
# 1. Copy this file: cp .env.example .env
# 2. Set LLM_API_KEY with your actual API key
# 3. Optionally customize provider, model, and parameters
# 4. For OpenAI: Make sure your API key has access to the specified model

# Provider Examples:
# OpenAI: LLM_PROVIDER=openai, LLM_MODEL=gpt-4o-mini
# Anthropic: LLM_PROVIDER=anthropic, LLM_MODEL=claude-3-haiku-20240307 (not implemented yet)
# Local: LLM_PROVIDER=local, LLM_BASE_URL=http://localhost:11434 (not implemented yet)
